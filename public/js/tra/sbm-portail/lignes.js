/**
 * 
 */
var js_lignes = (function(){
return {
	"via": function(visible) {
   			if (visible) {
   				$(".via").show();
   			} else {
   				$(".via").hide();
   			}
		
		}
	}
})();